int		ft_recursieve_power(int nb, int power)
{
	int	n;

	n = nb;
	if (power < 0)
		return (0);
	if (power == 0)
		return (1);
		
}

#include <stdio.h>
int		main()
{
	printf("%d\n". ft_recursieve_power(1024, 2));
	return (0);
}